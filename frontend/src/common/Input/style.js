import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    marginBottom: 8,
  },
  textInput: {
    height: 50,
    padding: 10,
    marginBottom: 12,
    fontSize: 20,
    borderWidth: 1,
  },
});

export default styles;
